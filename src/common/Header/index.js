import React from 'react';

import './index.css';

//Common header component.
const Header = (props) => {
    
    // Return JSX
    return (
        <header>
            React Weather Assignment
        </header>
    );
}

export default Header;
